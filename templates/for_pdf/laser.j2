{% macro build_list_of_subs(subs) %}
    <ul>
    {%- for sub in subs -%}
        <span>-> Стоимость абонимента: {{ sub.sub_gross }}</span></br>
    {%- endfor -%}
    </ul>
{% endmacro -%}
<b>{{ p.name }}</b>. Длительность (в минутах): {{ p.duration }}.{% if not p.manual_input %}{% if p.gross %}Разовое посещение. Стоимость: {{ p.gross }}{% else %} Абонимент(ы): {{ build_list_of_subs(p.subscriptions) }}{% endif %}{% else %} Сумма заработка введена вручную ({{ p.manual_value }}).{% endif %} Заработок: {{ p.earning }}
